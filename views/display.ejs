<% include partials/header %>


<h1>
    <%= campground.name %>
</h1>
<img src="<%= campground.image%> " alt="Image not found">
<p>
    <%= campground.description %>
</p>

<% campground.comments.forEach(function(comment){ %>
    <li><b><%= comment.author %></b> - <%= comment.text %></li>
<% }) %>


<% include partials/footer %>